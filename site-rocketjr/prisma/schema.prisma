generator client {
  provider = "prisma-client"
  output = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model ContactForms {
  id    Int @id @default(autoincrement())
  name  String
  email String
  telephone String
  enterprise String
  subject String
  message String
}