generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model ContactForms {
  id    Int @id @default(autoincrement())
  name  String
  email String @unique
  telephone String @unique
  enterprise String
  subject Json
  message String
}